--[[
    Made by Monnapse
--]]
--[=[
    @class manager
    @client
    The main directories.
]=]
local manager = {
    Info = require(script.info),
    NewManager = require(script.newmanager),
    Convert = require(script.convert),
    Inputs = require(script.inputs)
}

return manager